Вопросы с выбором варианта: 67/78%  
# Вопрос 1. Блок 5. Box model и управление отступами
  
### Содержание и вопросы

- Что входит в box model?
- Чем `margin` отличается от `padding`?
- Зачем нужен `box-sizing: border-box`?
- Почему элемент “не влезает” в ширину?

### Материал (лаконично)

- У каждого блока есть “коробка”.
- `padding` это воздух внутри блока. Он отодвигает текст от края рамки.
- `margin` это расстояние снаружи блока. Он раздвигает блоки друг от друга.
- Если ты задаешь элементу ширину и добавляешь padding и рамку, без `border-box` элемент может стать шире, чем ты ожидал.
- `box-sizing: border-box` делает ширину предсказуемой. Padding и рамка входят в заданную ширину.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Вы отметили, что материал прочитан. Это хорошее начало для понимания box model. Ключевые концепции — это разница между margin и padding, а также роль `box-sizing: border-box` для контроля размеров. Убедитесь, что вы можете объяснить эти термины своими словами и применить их на практике.  

#  Вопрос 2. Практическое задание (Блок 5)
  
### Условия

Наведи порядок в отступах пошагово:

1. В `styles.css` включи `border-box` для всех элементов одним правилом.
2. Выбери один основной вертикальный отступ и используй его для секций.
3. Применяй этот отступ ко всем секциям одинаково.
4. Для карточек проектов:
   - добавь внутренний отступ
   - добавь рамку или фон
   - добавь скругление углов
5. Открой страницу и проверь:
   - текст не прилипает к краям
   - расстояния между секциями выглядят одинаково

Ограничения:

- Отступы должны быть согласованными. Не делай “случайные” значения для каждого элемента.

### Псевдокод решения

```text
IN CSS_FILE
  APPLY BORDER_BOX_TO_ALL

  FOR EACH SECTION_BLOCK
    SET OUTER_SPACING

  FOR EACH CARD_BLOCK
    SET INNER_SPACING
    SET BORDER_OR_BG
    SET CORNER_ROUNDING
END
```

### Если использовал ИИ, обязательные изменения вручную

- Перестрой отступы так, чтобы они задавались через 1-2 “основных” правила, а не через множество точечных.
- Добавь правило, которое улучшает поведение изображений в карточках (без фиксированной ширины).

---

**Вставь решение в комментарий ниже:**
  
### 🟢 Ответ
*, *::before, *::after {  
  box-sizing: border-box;  
}  
  
section {  
  padding: 60px 20px;  
}  
  
.project-card {  
  padding: 24px;  
  background-color: #ffffff;  
  border: 1px solid #ddd;  
  border-radius: 12px;  
  margin-bottom: 20px;  
}  
  
.project-card img {  
  max-width: 100%;  
  height: auto;  
  display: block;  
  border-radius: 8px;  
}  
  
### ИИ анализ
Это отличное решение. Вы правильно применили `border-box` глобально, задали согласованный отступ для всех секций и оформили карточки с padding, рамкой и скруглением. Правило для изображений (`max-width: 100%`) идеально предотвращает выход картинки за пределы блока. Решение следует всем пунктам задания и демонстрирует понимание согласованных отступов и box model.  

# Вопрос 3. Что такое `padding` в CSS простыми словами (отступ внутри блока)?
  
  
Варианты ответов:
1) Отступ между блоками
2) Цвет рамки
3) Тип шрифта
4) Ссылка на файл
5) ✅ Отступ внутри блока
  
### ИИ анализ
Вы выбрали правильный вариант. `padding` — это внутренний отступ элемента, который создает пространство между его содержимым (например, текстом) и его границей (`border`). Это ключевая часть box model, которая влияет на визуальный комфорт и размеры блока. Понимание этого отличия от `margin` (внешнего отступа) критически важно для верстки.  

# Вопрос 4. Что такое `margin` в CSS простыми словами (отступ снаружи блока)?
  
  
Варианты ответов:
1) Название секции
2) Высота текста
3) Отступ снаружи блока
4) ❌ Отступ внутри блока
5) Размер картинки
  
### ИИ анализ
Вы выбрали неверный вариант. Правильный ответ — «Отступ снаружи блока». `margin` определяет внешнее пространство вокруг элемента, отодвигая его от соседних элементов. Ваш выбор («Отступ внутри блока») описывает `padding`. Важно четко различать эти два свойства, так как они управляют разными аспектами расположения элементов на странице.  

# Вопрос 5. Для чего обычно используют свойство `box-sizing: border-box` в CSS?
  
  
Варианты ответов:
1) Чтобы увеличить шрифт
2) Чтобы заменить flexbox
3) Чтобы отключить фон
4) Чтобы скрыть ссылки
5) ✅ Чтобы ширина была предсказуемой
  
### ИИ анализ
Вы дали абсолютно верный ответ. Свойство `box-sizing: border-box` изменяет модель расчета размеров элемента так, что заданная ширина (`width`) включает в себя `padding` и `border`. Это предотвращает неожиданное увеличение общего размера блока и делает верстку более контролируемой и предсказуемой.  

# Вопрос 6. Почему на странице может появиться горизонтальная полоса прокрутки из-за размеров элементов?
  
  
Варианты ответов:
1) `footer` пустой
2) `head` слишком большой
3) `title` слишком длинный
4) `meta` не тот
5) ✅ Элемент стал шире экрана
  
### ИИ анализ
Правильно. Горизонтальная полоса прокрутки чаще всего появляется, когда какой-либо элемент (например, изображение или блок с большим `padding`) выходит за пределы ширины области просмотра (`viewport`). Это часто происходит при использовании фиксированных ширины или отступов без `box-sizing: border-box`. Понимание этой причины помогает создавать адаптивные макеты.  

# Вопрос 7. Что значит “согласованные отступы” при работе с CSS?
  
  
Варианты ответов:
1) ✅ Одни и те же правила повторяются
2) Каждый блок со своим числом
3) Отступов нет нигде
4) Отступы только у ссылок
5) Отступы только у картинок
  
### ИИ анализ
Верно. «Согласованные отступы» означают использование единой системы значений (например, 20px, 40px) для `margin` и `padding` по всему сайту. Это создает визуальную гармонию, ритм и профессиональный вид интерфейса, в отличие от случайных, разных отступов для каждого элемента.  

# Вопрос 8. Что из перечисленного относится к CSS box model (модель блока)?
  
  
Варианты ответов:
1) ✅ margin, border, padding, content
2) title, meta, link, script
3) ul, ol, li, a
4) jpg, png, svg, gif
5) hover, active, visited, focus
  
### ИИ анализ
Вы правильно определили компоненты box model. Модель блока описывает, как каждый элемент на странице представлен в виде прямоугольной коробки, состоящей из содержимого (`content`), внутреннего отступа (`padding`), границы (`border`) и внешнего отступа (`margin`). Понимание этой модели — основа для управления layout в CSS.  

# Вопрос 9. Какое CSS-правило для изображений в карточках помогает избежать проблем с шириной блока?
  
  
Варианты ответов:
1) Картинка меняет язык страницы
2) Картинка добавляет заголовок
3) Картинка отключает рамку
4) ✅ Картинка не ломает ширину блока
5) Картинка становится ссылкой
  
### ИИ анализ
Вы выбрали правильный, хотя и сформулированный общими словами, вариант. Конкретное правило, которое решает эту проблему, — это `max-width: 100%`. Оно гарантирует, что изображение никогда не превысит ширину своего родительского контейнера, предотвращая появление горизонтальной прокрутки и сохраняя целостность макета карточки.  

Вопросы с текстовыми ответами: 100/22%
