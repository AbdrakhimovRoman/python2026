Вопросы с выбором варианта: 78/78%  
Вопросы с текстовыми ответами: 22/22%  
# Вопрос 1. Блок 6. Flexbox: выравнивание и простая сетка
  
### Содержание и вопросы

- Что такое flex-контейнер и flex-элемент?
- Чем отличаются `justify-content` и `align-items`?
- Что делает `gap`?
- Как сделать перенос карточек?

### Материал (лаконично)

- Flexbox это способ разложить элементы в ряд или в колонку и управлять выравниванием.
- Когда ты включаешь `display: flex`, дети внутри становятся flex-элементами.
- `justify-content` это распределение по главной оси. Обычно это горизонталь.
- `align-items` это выравнивание по другой оси. Обычно это вертикаль.
- `gap` это расстояние между элементами, без “костылей” с `margin`.
- `flex-wrap` позволяет элементам переноситься, если места не хватает.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Этот вопрос проверяет ознакомление с теоретическим материалом. Отметка "Прочитано" означает, что студент подтвердил изучение ключевых концепций Flexbox. Для успешного применения на практике важно не только прочитать, но и понять различия между свойствами `justify-content`, `align-items` и `flex-wrap`.  

#  Вопрос 2. Практическое задание (Блок 6)
  
### Условия

Сделай flex-верстку пошагово:

1. В HTML оберни содержимое шапки в отдельный блок или назначь класс шапке.
2. В `styles.css` включи для шапки `display: flex`.
3. Настрой выравнивание, чтобы:
   - аватар и текст стояли в одну строку
   - элементы были аккуратно выровнены
4. Добавь расстояние между аватаром и текстом.
5. Для секции проектов:
   - сделай контейнер карточек flex-контейнером
   - включи перенос
   - добавь промежутки
6. Настрой правило ширины карточек так, чтобы на широком экране было несколько карточек в ряд.
7. Сожми окно браузера и проверь, что карточки переносятся, а страница не ломается.

Ограничения:

- Сетка должна адекватно реагировать на сужение окна.

### Псевдокод решения

```text
IN CSS_FILE
  MAKE HEADER_ROW_LAYOUT
    SET DISPLAY_FLEX
    SET ALIGNMENT
    SET GAP

  MAKE CARDS_GRID
    SET DISPLAY_FLEX
    ENABLE WRAP
    SET GAP

  FOR EACH CARD
    SET FLEX_BASIS_OR_WIDTH_RULE
END
```

### Если использовал ИИ, обязательные изменения вручную

- Измени поведение сетки на узком экране (например, чтобы карточка становилась на всю ширину).
- Добавь выравнивание элементов в шапке так, чтобы визуально было ровно.

---

**Вставь решение в комментарий ниже:**
  
### 🟡 Ответ
<header class="header">  
    <img src="avatar.jpg" alt="Аватар" class="avatar">  
    <div class="header-info">  
        <h1>Имя Фамилия</h1>  
        <p>Frontend Developer</p>  
    </div>  
</header>  
  
<section class="projects">  
    <div class="projects-container">  
        <div class="card">Проект 1</div>  
        <div class="card">Проект 2</div>  
        <div class="card">Проект 3</div>  
    </div>  
</section>  
  
### ИИ анализ
Представлен корректный HTML-каркас для шапки и секции проектов, что является хорошим началом. Однако в ответе отсутствует критически важная часть — CSS-код, который реализует flexbox, выравнивание, перенос и отступы (`gap`). Без него невозможно оценить, выполнены ли условия задания по адаптивности и визуальному выравниванию. Для завершения работы необходимо добавить соответствующие стили в файл `styles.css`.  

# Вопрос 3. Что происходит с дочерними элементами, если у родительского контейнера стоит `display: flex`?
  
  
Варианты ответов:
1) Они отключают CSS
2) Они становятся `meta` тегами
3) ✅ Они становятся flex-элементами
4) Они превращаются в картинки
5) Они удаляются из страницы
  
### ИИ анализ
Правильный ответ выбран верно. Ключевая концепция Flexbox заключается в том, что при применении `display: flex` к родительскому контейнеру все его прямые дочерние элементы автоматически становятся flex-элементами. Это меняет модель их расположения, позволяя управлять выравниванием, порядком и распределением пространства вдоль главной и поперечной осей.  

# Вопрос 4. За что отвечает свойство `justify-content` в flexbox (распределение по главной оси)?
  
  
Варианты ответов:
1) За видимость картинки
2) За цвет фона
3) За размер шрифта
4) За тип списка
5) ✅ За распределение по главной оси
  
### ИИ анализ
Ответ правильный. Свойство `justify-content` управляет распределением flex-элементов вдоль главной оси контейнера (по умолчанию — горизонтальной). Оно позволяет выравнивать элементы в начале (`flex-start`), в конце (`flex-end`), по центру (`center`), равномерно распределять с отступами (`space-between`, `space-around`) или равномерно (`space-evenly`). Это одно из основных свойств для управления пространством внутри flex-контейнера.  

# Вопрос 5. За что отвечает свойство `align-items` в flexbox (выравнивание по поперечной оси)?
  
  
Варианты ответов:
1) За тип ссылки
2) За высоту контейнера
3) ✅ За выравнивание по другой оси
4) За количество элементов
5) За язык страницы
  
### ИИ анализ
Ответ верный. Свойство `align-items` отвечает за выравнивание flex-элементов вдоль поперечной оси (по умолчанию — вертикальной), которая перпендикулярна главной оси. Оно позволяет выровнять элементы по верхнему краю (`flex-start`), нижнему краю (`flex-end`), по центру (`center`), а также растянуть на всю высоту контейнера (`stretch`) или выровнять по базовой линии текста (`baseline`).  

# Вопрос 6. Что делает свойство `gap` в flexbox-контейнере?
  
  
Варианты ответов:
1) Добавляет тень
2) Скрывает элементы
3) Меняет цвет рамки
4) Увеличивает размер шрифта
5) ✅ Добавляет расстояние между элементами
  
### ИИ анализ
Ответ правильный. Свойство `gap` (или его устаревшие аналоги `row-gap` и `column-gap`) задает фиксированное расстояние (промежуток) между строками и колонками внутри flex- или grid-контейнера. Это современная и удобная альтернатива использованию полей (`margin`) для создания отступов между элементами, так как `gap` добавляет пространство только *между* элементами, не влияя на отступы по краям контейнера.  

# Вопрос 7. Какое свойство flexbox позволяет элементам переноситься на новую строку, если не хватает места?
  
  
Варианты ответов:
1) `text-align: center`
2) `position: absolute`
3) `float: left`
4) `display: block`
5) ✅ `flex-wrap: wrap`
  
### ИИ анализ
Ответ верный. Свойство `flex-wrap` управляет тем, будут ли flex-элементы принудительно оставаться в одной строке (или колонке) или смогут переноситься на новую строку при нехватке места. Значение `wrap` включает перенос, что критически важно для создания адаптивных сеток. Значение по умолчанию — `nowrap`, которое растягивает или сжимает элементы, чтобы они поместились в одну линию.  

# Вопрос 8. Что нужно проверить после настройки flexbox-сетки для карточек проектов?
  
  
Варианты ответов:
1) Что все заголовки одинаковые
2) Что все отступы нулевые
3) Что все ссылки ведут на один сайт
4) ✅ Что карточки переносятся при сужении
5) Что все картинки одинаковые
  
### ИИ анализ
Правильный ответ выбран верно. Основная цель создания адаптивной flexbox-сетки — обеспечить корректное отображение на экранах разной ширины. Поэтому после настройки необходимо проверить, что при сужении окна браузера карточки корректно переносятся на новую строку благодаря свойству `flex-wrap: wrap`, а макет не ломается. Это ключевой тест на адаптивность.  

# Вопрос 9. Зачем настраивать ширину карточек во flexbox-контейнере?
  
  
Варианты ответов:
1) Чтобы убрать рамки
2) Чтобы все были на одной строке
3) Чтобы все были разного размера
4) Чтобы скрыть текст
5) ✅ Чтобы было несколько в ряд на широком экране
  
### ИИ анализ
Ответ правильный. Без явного управления шириной (например, через `flex-basis`, `width`, `min-width` или `max-width`) flex-элементы будут пытаться равномерно заполнить доступное пространство в одной строке, что может привести к неконтролируемому растягиванию. Настройка ширины позволяет задать базовый размер карточек, чтобы на широком экране в строке помещалось несколько штук (например, 3 или 4), создавая предсказуемую и аккуратную сетку.
