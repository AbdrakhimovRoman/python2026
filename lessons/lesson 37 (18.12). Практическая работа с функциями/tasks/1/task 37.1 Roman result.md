result: 67/100

### 1. Формулировка задания
Требовалось реализовать три функции:
1. `rectangle_info(width, height)` — возвращает кортеж `(площадь, периметр)` прямоугольника.
2. `is_adult(age)` — возвращает `True`, если возраст ≥ 18, иначе `False`.
3. `safe_div(a, b)` — возвращает `a / b`, но если `b == 0`, возвращает `None`.

**Ограничения**: стандартные типы Python, обработка крайних случаев (деление на ноль).

---

### 2. Результаты проверки
**Файл**: `lessons/lesson 37 (18.12). Практическая работа с функциями/tasks/1/уяez`

- **Тест 1**: `rectangle_info(3, 4)`  
  **Ожидаемый вывод**: `(12, 14)`  
  **Фактический результат**: `NameError` из-за опечатки `heigh` вместо `height` (строка 3).  
  **Шаги воспроизведения**:  
  ```python
  print(rectangle_info(3, 4))  # Ошибка: NameError: name 'heigh' is not defined
  ```

- **Тест 2**: `is_adult(20)` → `True`, `is_adult(15)` → `False`  
  **Результат**: корректно (строка 7).

- **Тест 3**: `safe_div(10, 2)` → `5.0`, `safe_div(5, 0)` → `None`  
  **Результат**: корректно (строки 9–11).

---

### 3. Сильные стороны
- **Корректность**: Функции `is_adult` и `safe_div` реализованы верно.
- **Читаемость**: Код лаконичный, переменные названы осмысленно (`area`, `perimeter`).
- **Архитектура**: Соблюдена чистая функциональная парадигма (нет побочных эффектов).

---

### 4. Ошибки
**Блокирующая ошибка** (функция неработоспособна):
- **Опечатка в `rectangle_info`** (строка 3):  
  ```python
  area = width * heigh  # Ошибка: переменная 'heigh' не объявлена
  ```  
  **Исправление**:  
  ```python
  area = width * height  # Корректное имя переменной
  ```

---

### 5. Оценка
- **Функциональность**: 33/50  
  Одна из трёх функций неработоспособна из-за опечатки.
- **Качество кода**: 30/30  
  Нет логических ошибок (кроме опечатки), код структурирован.
- **Стиль и тесты**: 20/20  
  PEP-8 соблюдён, тесты не требовались по условию.

**Итог**: 67/100. Сняты баллы за нерабочую функцию `rectangle_info`.

---

### 6. Рекомендации
**Исправленный код для `rectangle_info`**:
```python
def rectangle_info(width, height):
    area = width * height  # Исправлено: heigh → height
    perimeter = 2 * (width + height)
    return area, perimeter
```

---

Анализ выполнен моделью: gpt-4
