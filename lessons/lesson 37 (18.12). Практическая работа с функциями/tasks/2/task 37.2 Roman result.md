result: 70/100

1. **Формулировка задания**  
   Требуется создать функцию `delivery_price(city, weight_kg, urgent)`, вычисляющую стоимость доставки:
   - При `urgent=True`: `500 + 30 * weight_kg`.
   - При `urgent=False`: `300 + 20 * weight_kg`.
   - Параметр `city` не влияет на расчёт, но присутствует для читаемости.  
   Необходимо вызвать функцию 4 раза: дважды позиционно и дважды с именованными аргументами. Также нужно добавить закомментированный вызов с ошибкой (например, неверный тип аргумента), чтобы продемонстрировать возникновение `TypeError` или `SyntaxError`.

2. **Результаты проверки**  
   - **Файл**: `lessons/lesson 37 (18.12). Практическая работа с функциями/tasks/2/123`  
   - **Корректность функции**: Реализация функции верна (строки 1–6).  
   - **Вызовы функции**:  
     - Позиционные вызовы (строки 8–9): `print(delivery_price("Москва", 10, True))` и `print(delivery_price("Санкт-Петербург", 5, False))` — корректны.  
     - Именованные вызовы отсутствуют. В строках 10–11 повторяются позиционные вызовы без указания имён аргументов.  
     - Ошибочный вызов (строка 13): `print(delivery_price("Москва", "10", True))` не закомментирован и приведёт к `TypeError` при выполнении (умножение строки на число).  
   - **Тесты**:  
     - Запуск кода вызовет ошибку в строке 13 из-за неверного типа `weight_kg`.

3. **Сильные стороны работы**  
   - Функция реализована корректно: логика расчёта соответствует условию.  
   - Код читаем: осмысленные имена параметров, нет избыточной сложности.  
   - Позиционные вызовы демонстрируют базовую работу функции.

4. **Ошибки**  
   **Блокирующие** (срыв основной функциональности):  
   - Отсутствуют именованные вызовы функции. Вместо них повторены позиционные (строки 10–11).  
     **Исправление**:  
     ```python
     print(delivery_price(city="Москва", weight_kg=10, urgent=True))
     print(delivery_price(city="Санкт-Петербург", weight_kg=5, urgent=False))
     ```  
   - Ошибочный вызов не закомментирован (строка 13), что приводит к ошибке при выполнении.  
     **Исправление**: Закомментировать строку и добавить пояснение:  
     ```python
     # print(delivery_price("Москва", "10", True))  # TypeError: неправильный тип weight_kg
     ```  

   **Значимые** (нарушение условий задания):  
   - Не выполнено требование по двум именованным вызовам.  

5. **Оценка**  
   - **Функциональность (50%)**: 30/50.  
     Функция работает, но вызовы не соответствуют условиям (нет именованных, ошибочный вызов не закомментирован).  
   - **Качество кода (30%)**: 25/30.  
     Незначительные стилевые недочёты (лишние пробелы в строках 8–9).  
   - **Стиль и тесты (20%)**: 15/20.  
     Нет комментария к ошибочному вызову, нарушена структура вызовов.  
   **Итого**: 70/100.

6. **Рекомендации**  
   Для полного выполнения задания:  
   - Добавьте два именованных вызова функции.  
   - Закомментируйте ошибочный вызов с пояснением ошибки.  

Пример исправленного кода:  
```python
def delivery_price(city, weight_kg, urgent):
    if urgent:
        return 500 + 30 * weight_kg
    else:
        return 300 + 20 * weight_kg

# Позиционные вызовы
print(delivery_price("Москва", 10, True))
print(delivery_price("Санкт-Петербург", 5, False))

# Именованные вызовы
print(delivery_price(city="Москва", weight_kg=10, urgent=True))
print(delivery_price(city="Санкт-Петербург", weight_kg=5, urgent=False))

# Закомментированный ошибочный вызов
# print(delivery_price("Москва", "10", True))  # TypeError: weight_kg должен быть числом
```

---

Анализ выполнен моделью: GPT-4o.
