Вопросы с выбором варианта: 83/100%  
# Вопрос 1. Тема 1: Работа с форматами CSV, JSON
  
Что такое JSON?
  
Варианты ответов:
1) Формат изображений
2) Архив файлов
3) Табличный формат данных
4) ✅ Текстовый формат данных
5) Язык программирования
  
### ИИ анализ
JSON (JavaScript Object Notation) — это легкий текстовый формат для обмена данными, основанный на синтаксисе объектов JavaScript.  Он использует структуры из пар ключ-значение и упорядоченных списков, что делает его понятным для человека и машины.  Правильный ответ подчеркивает его текстовую природу, в отличие от бинарных форматов.  

# Вопрос 2. Тема 1: Работа с форматами CSV, JSON
  
Какой тип данных в JSON соответствует словарю Python?
  
Варианты ответов:
1) number
2) string
3) null
4) ✅ object
5) boolean
  
### ИИ анализ
В JSON тип `object` представляет собой неупорядоченный набор пар "ключ: значение", заключенный в фигурные скобки `{}`.  Это прямая аналогия словарю (dict) в Python, где данные также хранятся в виде ключей и соответствующих им значений.  Другие типы JSON, такие как `array` или `string`, соответствуют другим структурам данных в Python.  

# Вопрос 3. Тема 1: Работа с форматами CSV, JSON
  
Какой тип данных в JSON соответствует списку Python?
  
Варианты ответов:
1) object
2) string
3) ✅ array
4) null
5) date
  
### ИИ анализ
Тип `array` в JSON представляет собой упорядоченную коллекцию значений, заключенную в квадратные скобки `[]`.  Это напрямую соответствует списку (list) в Python, который также является упорядоченной и изменяемой последовательностью элементов.  Важно не путать `array` с `object`, который соответствует словарю.  

# Вопрос 4. Тема 1: Работа с форматами CSV, JSON
  
Какая функция чаще всего используется, чтобы превратить JSON-строку в Python-объект?
  
Варианты ответов:
1) json.load()
2) ✅ json.loads()
3) json.parse()
4) json.dump()
5) json.dumps()
  
### ИИ анализ
Функция `json.loads()` (load string) предназначена для десериализации (парсинга) строки JSON в объект Python.  Её важно отличать от `json.load()`, которая читает JSON из файлового объекта.  Функции `json.dump()` и `json.dumps()` выполняют обратную операцию — сериализацию.  

# Вопрос 5. Тема 1: Работа с форматами CSV, JSON
  
Какая функция чаще всего используется, чтобы превратить Python-объект в JSON-строку?
  
Варианты ответов:
1) json.stringify()
2) json.loads()
3) json.load()
4) ✅ json.dumps()
5) json.dump()
  
### ИИ анализ
Функция `json.dumps()` (dump string) сериализует (преобразует) объект Python в строку в формате JSON.  Её следует отличать от `json.dump()`, которая записывает результат в файл.  Функции `json.load()` и `json.loads()` выполняют обратную операцию десериализации.  

# Вопрос 6. Тема 1: Работа с форматами CSV, JSON
  
Что такое CSV?
  
Варианты ответов:
1) Формат для кода
2) Текстовый табличный формат
3) Тип базы данных
4) ❌ Формат дерева данных
5) Формат видеороликов
  
### ИИ анализ
CSV (Comma-Separated Values) — это простой текстовый формат для представления табличных данных, где значения разделены запятыми (или другими разделителями).  Каждая строка файла соответствует строке таблицы, а значения в строке — ячейкам.  Выбранный вариант "Формат дерева данных" неверен, так как CSV представляет плоскую таблицу, а не иерархическую структуру.  

# Вопрос 7. Тема 1: Работа с форматами CSV, JSON
  
Какой модуль Python чаще всего используют для чтения CSV?
  
Варианты ответов:
1) json
2) re
3) math
4) time
5) ✅ csv
  
### ИИ анализ
Стандартный модуль `csv` в Python предоставляет функционал для чтения и записи файлов в формате CSV.  Он содержит такие классы, как `csv.reader` и `csv.DictReader`, которые упрощают работу с табличными данными.  Другие перечисленные модули предназначены для иных задач (работа с JSON, регулярными выражениями и т.д.).  

# Вопрос 8. Тема 2: Основы тестирования
  
Что такое тест в программировании?
  
Варианты ответов:
1) ✅ Проверка правильной работы
2) Любая ошибка в коде
3) Встроенная команда
4) Способ ускорить код
5) Файл с настройками
  
### ИИ анализ
Тест в программировании — это процедура, которая проверяет, соответствует ли поведение определенной части кода (функции, модуля) ожидаемому.  Его цель — выявить ошибки и убедиться в корректности работы.  Это не ошибка сама по себе и не инструмент для оптимизации производительности.  

# Вопрос 9. Тема 2: Основы тестирования
  
Что чаще всего проверяет unit-тест (модульный тест)?
  
Варианты ответов:
1) Дизайн интерфейса
2) Скорость интернета
3) Одну функцию или модуль
4) ❌ Работу всей программы
5) Версию системы
  
### ИИ анализ
Unit-тест (модульный тест) предназначен для проверки изолированной, минимальной части кода — обычно одной функции, метода или небольшого модуля.  Его цель — убедиться, что эта единица логики работает правильно.  Проверка работы всей программы — это задача интеграционного или системного тестирования.  

# Вопрос 10. Тема 2: Основы тестирования
  
Зачем нужны автоматические тесты?
  
Варианты ответов:
1) Чтобы заменить комментарии
2) ✅ Чтобы не ломать при правках
3) Чтобы убрать все исключения
4) Чтобы работать офлайн
5) Чтобы уменьшить размер
  
### ИИ анализ
Автоматические тесты позволяют быстро и надежно проверять корректность кода после любых изменений (рефакторинга, добавления нового функционала).  Они служат "страховочной сеткой", которая помогает предотвратить регрессию — появление старых ошибок или поломку существующей функциональности.  

# Вопрос 11. Тема 2: Основы тестирования
  
Что такое assert?
  
Варианты ответов:
1) Команда печати
2) Способ импортировать
3) Команда выхода из цикла
4) Команда ввода данных
5) ✅ Проверка условия в коде
  
### ИИ анализ
Инструкция `assert` в Python проверяет заданное условие.  Если условие истинно, выполнение кода продолжается.  Если ложно, вызывается исключение `AssertionError`.  Она широко используется в тестах для проверки ожидаемых результатов, но также может применяться для отладки в основном коде.  

# Вопрос 12. Тема 2: Основы тестирования
  
Что обычно означает «ожидаемый результат» в тесте?
  
Варианты ответов:
1) ✅ То, что должно получиться
2) Любое случайное число
3) Результат всегда 0
4) Скорость программы
5) Имя файла тестов
  
### ИИ анализ
"Ожидаемый результат" (expected result) — это конкретное, заранее известное значение или состояние, которое должна вернуть тестируемая часть кода при заданных входных данных.  Сравнение фактического результата с ожидаемым является сутью любого автоматизированного теста.  

# Вопрос 13. Тема 2: Основы тестирования
  
Что такое тест-кейс?
  
Варианты ответов:
1) Ошибка в коде
2) Команда для запуска Python
3) Тип данных
4) ✅ Вход и ожидаемый итог
5) Список модулей проекта
  
### ИИ анализ
Тест-кейс — это формализованный сценарий проверки, который включает в себя набор входных данных, условий выполнения и ожидаемый результат.  Он описывает, *что* тестировать, *как* запускать тест и *какой* результат считать правильным для конкретной функциональности.  

# Вопрос 14. Тема 2: Основы тестирования
  
Почему полезно писать тесты до исправления бага?
  
Варианты ответов:
1) Чтобы сменить стиль
2) ❌ Чтобы удалить баги
3) Чтобы ускорить код
4) Чтобы баг был заметнее
5) Чтобы проверить исправление
  
### ИИ анализ
Написание теста, который воспроизводит баг *до* его исправления, позволяет четко зафиксировать некорректное поведение.  После внесения правок этот же тест подтвердит, что баг действительно устранен.  Сам по себе тест не удаляет баги, но является инструментом для их контроля и верификации исправлений.  

# Вопрос 15. Тема 3: Рекурсия
  
Что такое рекурсия?
  
Варианты ответов:
1) Повторение цикла for
2) Когда создают класс
3) ✅ Когда функция вызывает сама себя
4) Когда импортируют модуль
5) Когда используют список
  
### ИИ анализ
Рекурсия — это подход в программировании, при котором функция для решения задачи вызывает саму себя, но с другими (обычно более простыми) аргументами.  Это позволяет элегантно решать задачи, которые можно разбить на идентичные подзадачи меньшего размера, например, обход деревьев или вычисление факториала.  

# Вопрос 16. Тема 3: Рекурсия
  
Что обязательно должно быть в рекурсивной функции?
  
Варианты ответов:
1) input()
2) global
3) ✅ Базовый случай
4) try/except
5) print()
  
### ИИ анализ
Базовый случай (терминальное условие) — это обязательный элемент любой корректной рекурсивной функции.  Он определяет простейшую задачу, которая решается напрямую без рекурсивного вызова, и останавливает цепочку рекурсии.  Без базового случая рекурсия будет бесконечной и приведет к ошибке.  

# Вопрос 17. Тема 3: Рекурсия
  
Что произойдет, если в рекурсивной функции нет условия остановки?
  
Варианты ответов:
1) ❌ Python сам остановит
2) Выполнится один раз
3) Создаст файл
4) Будет ошибка RecursionError
5) Станет быстрее
  
### ИИ анализ
При отсутствии условия остановки (базового случая) функция будет вызывать саму себя бесконечно, пока не будет исчерпан стек вызовов.  В Python это приводит к исключению `RecursionError`, которое и прерывает выполнение программы.  Интерпретатор не может автоматически определить логическую остановку рекурсии.  

# Вопрос 18. Тема 3: Рекурсия
  
Какой пример чаще всего приводят для обучения рекурсии?
  
Варианты ответов:
1) Работа с SQL
2) Чтение CSV
3) Запись JSON
4) Сортировка пузырьком
5) ✅ Факториал числа
  
### ИИ анализ
Вычисление факториала числа (n! = n * (n-1)!) — это классический учебный пример рекурсии, потому что он наглядно демонстрирует принцип: задача сводится к такой же, но более простой задаче.  Базовый случай здесь — факториал 0 или 1, равный 1.  Этот пример прост для понимания математической основы рекурсии.  

# Вопрос 19. Тема 3: Рекурсия
  
Чем рекурсия обычно отличается от цикла?
  
Варианты ответов:
1) Разницы нет
2) Рекурсия быстрее
3) Только с числами
4) ✅ Через вызовы функции
5) Цикл только while
  
### ИИ анализ
Ключевое отличие рекурсии от итерации (цикла) в механизме выполнения: рекурсия использует вложенные вызовы функций и опирается на системный стек для хранения состояния каждого вызова.  Цикл же управляет повторениями с помощью управляющих переменных и условий внутри одной и той же области выполнения.  

# Вопрос 20. Тема 3: Рекурсия
  
Что такое «глубина рекурсии»?
  
Варианты ответов:
1) ✅ Число вложенных вызовов
2) Количество переменных
3) Количество строк
4) Количество файлов
5) Количество импортов
  
### ИИ анализ
Глубина рекурсии — это максимальное количество одновременных, вложенных вызовов рекурсивной функции, находящихся в стеке вызовов в один момент времени.  Она зависит от входных данных и логики функции.  В Python существует ограничение на глубину рекурсии, которое можно изменить.  

# Вопрос 21. Тема 3: Рекурсия
  
Какой стандартный модуль позволяет узнать или изменить лимит рекурсии?
  
Варианты ответов:
1) math
2) csv
3) json
4) random
5) ✅ sys
  
### ИИ анализ
Модуль `sys` предоставляет функцию `getrecursionlimit()` для получения текущего максимального предела глубины рекурсии и `setrecursionlimit()` для его изменения.  Это системные настройки интерпретатора Python.  Изменять лимит следует с осторожностью, чтобы избежать переполнения стека и краха программы.  

# Вопрос 22. Тема 4: Логирование, работа с датой и временем
  
Для чего обычно используют модуль logging?
  
Варианты ответов:
1) Чтобы читать CSV
2) Чтобы хранить пароли
3) Чтобы ускорять код
4) ✅ Чтобы писать сообщения в лог
5) Чтобы рисовать графики
  
### ИИ анализ
Модуль `logging` предназначен для записи логов — структурированных сообщений о событиях, происходящих во время работы программы.  Он позволяет фиксировать информацию разного уровня важности (от отладочной до критической) и направлять её в различные источники (консоль, файлы, сеть).  

# Вопрос 23. Тема 4: Логирование, работа с датой и временем
  
Какой уровень логирования обычно используют для сообщений об ошибках?
  
Варианты ответов:
1) DEBUG
2) TRACE
3) WARNING
4) ✅ ERROR
5) INFO
  
### ИИ анализ
Уровень `ERROR` используется для регистрации серьезных проблем, из-за которых программа не смогла выполнить какую-либо функцию.  Это более высокий уровень, чем `WARNING` (предупреждение) и `INFO` (информация), и более низкий, чем `CRITICAL`.  Он указывает на сбой в работе, требующий внимания.  

# Вопрос 24. Тема 4: Логирование, работа с датой и временем
  
Как получить текущие дату и время в Python через datetime?
  
Варианты ответов:
1) datetime.date()
2) ✅ datetime.now()
3) datetime.current()
4) datetime.time()
5) datetime.today (без скобок)
  
### ИИ анализ
Метод `datetime.now()` класса `datetime` из одноименного модуля возвращает объект `datetime`, представляющий текущую локальную дату и время с учетом часового пояса (если он передан).  `datetime.today()` — это похожий метод, но он не принимает параметр часового пояса.  

# Вопрос 25. Тема 4: Логирование, работа с датой и временем
  
Какой тип возвращает datetime.now()?
  
Варианты ответов:
1) int
2) list
3) bool
4) str
5) ✅ datetime
  
### ИИ анализ
Метод `datetime.now()` возвращает объект типа `datetime.datetime`.  Этот объект содержит атрибуты для года, месяца, дня, часа, минуты, секунды и микросекунды, а также методы для манипуляций с датой и временем (сложение, вычитание, форматирование).  

# Вопрос 26. Тема 4: Логирование, работа с датой и временем
  
Для чего нужен strftime?
  
Варианты ответов:
1) Разбить строку в список
2) Открыть файл
3) Проверить regex
4) ✅ Форматировать дату в строку
5) Создать JSON
  
### ИИ анализ
Метод `strftime()` (string format time) преобразует объект `datetime` или `date` в строковое представление согласно заданному формату.  Пользователь указывает шаблон с помощью специальных кодов (например, `%Y` для года, `%m` для месяца), чтобы получить дату в нужном виде.  

# Вопрос 27. Тема 4: Логирование, работа с датой и временем
  
Для чего нужен strptime?
  
Варианты ответов:
1) Создать цикл
2) ✅ Разобрать строку в дату
3) Записать лог
4) Сменить кодировку
5) Посчитать сумму
  
### ИИ анализ
Метод `strptime()` (string parse time) выполняет обратную операцию по отношению к `strftime()`: он разбирает строку, представляющую дату и время, и создает на её основе объект `datetime`.  Для этого необходимо точно указать шаблон формата, которому соответствует входная строка.  

# Вопрос 28. Тема 4: Логирование, работа с датой и временем
  
Что обычно записывают в лог вместо print() в готовых программах?
  
Варианты ответов:
1) HTML теги
2) ✅ Сообщения о работе кода
3) Математические формулы
4) Пароли пользователя
5) Номера телефонов
  
### ИИ анализ
В лог записывают структурированные сообщения о событиях в программе: старт и остановка, успешное выполнение операций, предупреждения, ошибки и критические сбои.  В отличие от `print()`, логирование предоставляет уровни важности, ротацию файлов, разные цели вывода и не "засоряет" стандартный вывод.  

# Вопрос 29. Тема 5: Регулярные выражения
  
Для чего чаще всего используют регулярные выражения?
  
Варианты ответов:
1) ✅ Для поиска по шаблону
2) Для создания файлов
3) Для ускорения компьютера
4) Для рисования интерфейса
5) Для работы с БД
  
### ИИ анализ
Регулярные выражения (regex) — это мощный инструмент для обработки текста, позволяющий находить, извлекать, проверять и заменять подстроки по сложным шаблонам.  Они используются для валидации данных (емейлов, телефонов), парсинга логов, извлечения информации из текста и сложного поиска с заменой.  

# Вопрос 30. Тема 5: Регулярные выражения
  
Какой модуль в Python отвечает за регулярные выражения?
  
Варианты ответов:
1) str
2) text
3) ✅ re
4) match
5) regex
  
### ИИ анализ
Стандартный модуль `re` в Python предоставляет полный набор функций для работы с регулярными выражениями, такие как `re.search()`, `re.match()`, `re.findall()`, `re.sub()`.  Он является основной библиотекой для операций сопоставления с шаблоном и манипуляций с текстом на основе regex.  

# Вопрос 31. Тема 5: Регулярные выражения
  
Что означает шаблон \d?
  
Варианты ответов:
1) ✅ Любая цифра
2) Любой пробел
3) Точка
4) Любая буква
5) Конец строки
  
### ИИ анализ
Специальная последовательность `\d` в регулярных выражениях соответствует любой десятичной цифре (эквивалент классу `[0-9]`).  Это один из наиболее часто используемых шаблонов для поиска числовой информации в тексте.  Для поиска не-цифр используется `\D`.  

# Вопрос 32. Тема 5: Регулярные выражения
  
Что означает символ . в регулярном выражении?
  
Варианты ответов:
1) Только точку
2) ✅ Любой один символ
3) Любую цифру
4) Только запятую
5) Начало строки
  
### ИИ анализ
Символ точки `.` в regex является метасимволом, который соответствует любому *одному* символу, за исключением символа новой строки (если не используется флаг `re.DOTALL`).  Чтобы найти именно символ точки, его необходимо экранировать обратным слэшем: `\.`.  

# Вопрос 33. Тема 5: Регулярные выражения
  
Что означает ^ в начале шаблона?
  
Варианты ответов:
1) Любой символ
2) Отрицание класса символов
3) Разделитель
4) ✅ Начало строки
5) Конец строки
  
### ИИ анализ
Метасимвол `^` (каретка) в начале регулярного выражения означает "начало строки".  Он привязывает совпадение к старту текстовой строки.  Важно не путать эту роль с его использованием внутри квадратных скобок `[^...]`, где он означает отрицание (инверсию) класса символов.  

# Вопрос 34. Тема 5: Регулярные выражения
  
Что означает $ в конце шаблона?
  
Варианты ответов:
1) Пробел
2) Начало строки
3) Любая буква
4) Любая цифра
5) ✅ Конец строки
  
### ИИ анализ
Метасимвол `$` в конце регулярного выражения означает "конец строки".  Он привязывает совпадение к завершению текстовой строки.  Использование `^` в начале и `$` в конце шаблона позволяет проверить, соответствует ли ему вся строка целиком, а не просто содержит его.
# Вопрос 35. Тема 5: Регулярные выражения
  
Какая функция модуля re чаще всего проверяет, что строка начинается с шаблона?
  
Варианты ответов:
1) re.splitlines()
2) re.cut()
3) ✅ re.match()
4) re.parse()
5) re.find()
  
### ИИ анализ
Функция `re.match()` специально предназначена для проверки соответствия шаблона с самого начала строки.  Она возвращает объект совпадения, если шаблон найден в начале, или `None`, если нет.  Это отличает её от `re.search()`, который ищет шаблон в любом месте строки.

# Вопрос 36. Тема 6: Основы асинхронного программирования
  
Что обычно означает «асинхронность» в программировании?
  
Варианты ответов:
1) Всегда быстрее
2) Не ждать одну задачу
3) Нельзя тестировать
4) Только одно ядро
5) Только про файлы
6) ⚡ (0.2/1) Я не знаю ответа
  
### ИИ анализ
Вы выбрали "Я не знаю ответа".  Ключевая идея асинхронности — это способность программы не блокироваться на выполнении одной длительной операции (например, ожидании ответа от сети), а переключаться на выполнение других задач.  Это повышает общую эффективность при работе с операциями ввода-вывода.

# Вопрос 37. Тема 6: Основы асинхронного программирования
  
Для чего чаще всего полезен asyncio?
  
Варианты ответов:
1) Для JSON
2) Для тяжелой математики
3) ✅ Для задач с ожиданием
4) Для списков
5) Для рисования графики
  
### ИИ анализ
Правильно.  Библиотека `asyncio` в Python оптимизирована для задач, связанных с ожиданием (I/O-bound), таких как сетевые запросы, чтение файлов или взаимодействие с базами данных.  Она позволяет эффективно управлять множеством таких операций, не создавая отдельные потоки для каждой.

# Вопрос 38. Тема 6: Основы асинхронного программирования
  
Как объявить асинхронную функцию в Python?
  
Варианты ответов:
1) async function f():
2) def f() async:
3) await def f():
4) def async f():
5) async def f():
6) ⚡ (0.2/1) Я не знаю ответа
  
### ИИ анализ
Вы выбрали "Я не знаю ответа".  Асинхронная функция в Python объявляется с использованием ключевых слов `async def`.  Это указывает интерпретатору, что функция является корутиной и может содержать операции `await`.  Например: `async def fetch_data():`.

# Вопрос 39. Тема 6: Основы асинхронного программирования
  
Что делает await?
  
Варианты ответов:
1) Завершает программу
2) ✅ Ожидает завершения асинхронной операции
3) Импортирует модуль
4) Печатает результат
5) Создает список
  
### ИИ анализ
Верно.  Ключевое слово `await` приостанавливает выполнение текущей корутины до тех пор, пока переданная ей асинхронная операция (другая корутина, задача или future) не завершится.  Это позволяет программе в это время выполнять другие задачи, не блокируя весь поток выполнения.

# Вопрос 40. Тема 6: Основы асинхронного программирования
  
Что такое «корутина» (coroutine) в Python?
  
Варианты ответов:
1) Тип списка
2) Ошибка кода
3) ✅ Функция async def
4) Файл настроек
5) Число int
  
### ИИ анализ
Правильно.  Корутина — это специальный тип функции, объявленный с помощью `async def`.  Её выполнение может быть приостановлено (с помощью `await`) и позже возобновлено.  Корутины являются фундаментальными строительными блоками для асинхронного кода в Python.

# Вопрос 41. Тема 6: Основы асинхронного программирования
  
Как чаще всего запускают главный asyncio-код в простых программах?
  
Варианты ответов:
1) asyncio.wait_all(main)
2) run.asyncio(main())
3) main(asyncio)
4) asyncio.start(main())
5) ✅ asyncio.run(main())
  
### ИИ анализ
Верно.  Функция `asyncio.run()` является стандартным и рекомендуемым способом запуска главной асинхронной функции (корутины) в программе.  Она создаёт цикл событий, выполняет переданную корутину и корректно закрывает цикл по завершении.

# Вопрос 42. Тема 6: Основы асинхронного программирования
  
Что обычно значит «конкурентное выполнение» в asyncio?
  
Варианты ответов:
1) ✅ Задачи чередуются в ожидании
2) Пишут в один файл
3) Всегда потоки
4) Только в фоне
5) Всегда процессы
  
### ИИ анализ
Правильно.  В контексте `asyncio` конкурентность означает, что несколько задач выполняются, переключаясь друг с другом в одном потоке.  Переключение происходит в моменты, когда задача встречает операцию `await` (ожидание).  Это отличается от параллельного выполнения на нескольких ядрах процессора.

# Вопрос 43. Тема 7: SQL, база и многотабличные запросы
  
Что такое база данных (БД)?
  
Варианты ответов:
1) Excel таблица
2) Компилятор
3) ✅ Хранилище данных
4) Язык программирования
5) Текстовый файл
  
### ИИ анализ
Верно.  База данных — это организованное, структурированное хранилище данных, предназначенное для эффективного управления, обновления и извлечения информации.  Она управляется системой управления базами данных (СУБД), которая обеспечивает надёжность, безопасность и целостность данных.

# Вопрос 44. Тема 7: SQL, база и многотабличные запросы
  
Какой SQL-запрос обычно используют, чтобы получить данные из таблицы?
  
Варианты ответов:
1) FETCHALL
2) READ
3) GET
4) OPEN
5) ✅ SELECT
  
### ИИ анализ
Правильно.  Команда `SELECT` является основным оператором языка SQL для запроса данных из одной или нескольких таблиц.  Она позволяет указать, какие столбцы нужно вернуть, из каких таблиц и при каких условиях (с помощью `WHERE`), а также как отсортировать или сгруппировать результат.

# Вопрос 45. Тема 7: SQL, база и многотабличные запросы
  
Какой SQL-запрос обычно используют, чтобы добавить новую строку в таблицу?
  
Варианты ответов:
1) APPEND
2) PUSH
3) CREATE
4) ❌ ADD
5) INSERT
  
### ИИ анализ
Вы выбрали неверный вариант "ADD".  Правильная команда для добавления новой строки (записи) в таблицу — `INSERT`.  Её синтаксис: `INSERT INTO table_name (column1, column2) VALUES (value1, value2);`.  Команда `CREATE` используется для создания новых объектов (таблиц, баз данных).

# Вопрос 46. Тема 7: SQL, база и многотабличные запросы
  
Какой SQL-запрос обычно используют, чтобы изменить существующие строки?
  
Варианты ответов:
1) ALTER ROW
2) CHANGE
3) MODIFY
4) EDIT
5) ✅ UPDATE
  
### ИИ анализ
Верно.  Команда `UPDATE` используется для модификации существующих данных в таблице.  Она позволяет задать новые значения для указанных столбцов и обязательно должна включать условие `WHERE`, чтобы точно определить, какие строки нужно изменить, иначе будут обновлены все строки таблицы.

# Вопрос 47. Тема 7: SQL, база и многотабличные запросы
  
Что делает WHERE в SQL?
  
Варианты ответов:
1) Создает таблицу
2) Удаляет таблицу
3) Сортирует строки
4) ✅ Фильтрует по условию
5) Объединяет строки
  
### ИИ анализ
Правильно.  Ключевое слово `WHERE` используется в командах `SELECT`, `UPDATE` и `DELETE` для фильтрации записей.  Оно задаёт условие, которому должна соответствовать строка, чтобы попасть в результирующий набор или быть изменённой/удалённой.  Например: `SELECT * FROM users WHERE age > 18;`.

# Вопрос 48. Тема 7: SQL, база и многотабличные запросы
  
Для чего нужен JOIN?
  
Варианты ответов:
1) Удалить столбец
2) Посчитать сумму
3) Соединить две таблицы
4) Создать базу
5) Переименовать таблицу
6) ⚡ (0.2/1) Я не знаю ответа
  
### ИИ анализ
Вы выбрали "Я не знаю ответа".  Оператор `JOIN` используется для объединения строк из двух или более таблиц на основе логической связи (соотношения) между ними.  Это фундаментальная операция в реляционных базах данных, позволяющая извлекать связанные данные, разнесённые по разным таблицам, в один результат.

# Вопрос 49. Тема 7: SQL, база и многотабличные запросы
  
Что чаще всего указывают в условии соединения таблиц (ON) при JOIN?
  
Варианты ответов:
1) Два случайных числа
2) Команда SELECT *
3) Имя базы данных
4) Название файла
5) Равенство связанных полей
6) ⚡ (0.2/1) Я не знаю ответа
  
### ИИ анализ
Вы выбрали "Я не знаю ответа".  В условии `ON` для `JOIN` почти всегда указывается условие равенства между связанными полями (столбцами) из соединяемых таблиц.  Например, `ON users.id = orders.user_id`.  Это условие определяет, как строки одной таблицы соотносятся со строками другой.
