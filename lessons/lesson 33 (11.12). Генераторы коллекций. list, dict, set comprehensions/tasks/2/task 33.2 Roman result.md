result: 35/100

1. **Формулировка задания**  
Требуется создать список строк формата `"<имя>: <задача> (X мин)"` для элементов, где длительность больше или равна медиане списка `durations`. Использовать **одно выражение list comprehension** с одновременным обходом трёх списков через `zip`. Медиану рассчитать заранее. Сохранить исходный порядок элементов.

2. **Результаты проверки**  
- **Файл:** `lessons/lesson 33 (11.12). Генераторы коллекций. list, dict, set comprehensions/tasks/2/ne pravilno`  
  **Строки:**  
  ```python
  result = []
  for task, owner, duration in zip(tasks, owners, durations):
      if duration <= median:
          result.append(f"{owner}: {task} ({duration} мин)")
  ```  
  - **Ошибки:**  
    - Использован цикл `for` вместо list comprehension (нарушение основного требования).  
    - Условие `duration <= median` вместо `>=` (логика фильтрации обратная).  
    - Медиана рассчитана некорректно для списков чётной длины (например, для `durations = [30, 40, 50, 60]` медиана должна быть 45, но код студента вернёт 40).  

3. **Сильные стороны**  
- Правильно использован `zip` для одновременного обхода списков.  
- Формат строки соответствует требованию (`"Имя: задача (N мин)"`).  
- Медиана рассчитана до основного кода (хотя и с ошибкой).

4. **Ошибки**  
**Блокирующие:**  
1. Отсутствие list comprehension. Вместо него использован цикл `for`.  
   **Исправление:** Заменить цикл на list comprehension.  
2. Неверное условие фильтрации (`<=` вместо `>=`).  
   **Исправление:** Изменить условие на `if duration >= median`.  
3. Некорректный расчёт медианы для чётных списков.  
   **Исправление:**  
   ```python
   sorted_durations = sorted(durations)
   n = len(sorted_durations)
   median = (sorted_durations[n//2] + sorted_durations[(n-1)//2]) / 2
   ```  

**Значимые:**  
1. Опечатка в примере данных (`"делой"` вместо `"деплой"`), но это не влияет на логику.  

5. **Оценка**  
- **Функциональность (50%): 20/50**  
  - Нет list comprehension (-15).  
  - Обратная логика фильтрации (-10).  
  - Медиана рассчитана некорректно (-5).  
- **Качество кода (30%): 10/30**  
  - Нарушены ключевые требования, но структура частично верна.  
- **Стиль и тесты (20%): 5/20**  
  - Нет тестов, стиль приемлем, но есть логические ошибки.  

6. **Исправленное решение**  
```python
tasks = ["разбор логов", "Code review", "деплой"]
owners = ["Анна", "Борис", "Вика"]
durations = [38, 55, 48]

# Корректный расчёт медианы для любой длины списка
sorted_durations = sorted(durations)
n = len(sorted_durations)
median = (sorted_durations[n//2] + sorted_durations[(n-1)//2]) / 2

# List comprehension с условием >=
result = [
    f"{owner}: {task} ({duration} мин)"
    for task, owner, duration in zip(tasks, owners, durations)
    if duration >= median
]
print(result)
```

**Модель ИИ:** GPT-4o
