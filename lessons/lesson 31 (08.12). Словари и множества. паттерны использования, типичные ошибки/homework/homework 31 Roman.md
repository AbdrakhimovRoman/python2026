# Домашнее задание. Урок 31. Словари и множества. паттерны использования, типичные ошибки

## Студент: Roman

По результатам урока у тебя есть хорошие решения по блокам 2 и 3, но часть базовой практики по словарям и множествам ещё стоит закрепить.

## Задача 1. Базовый словарь заказов

Пример входных данных:
```python
orders = [
    ("pizza", 2),
    ("sushi", 1),
    ("pizza", 3),
]
```

### Требуется
1. Собрать словарь `order_count`, где ключ. название блюда, значение. суммарное количество заказов.
2. Вывести все пары в формате `блюдо -> количество`.

---

## Задача 2. Безопасное удаление записей

Пример входных данных:
```python
balances = [
    ("alice", 1000),
    ("bob", 500),
    ("alice", -200),
]
closed = ["bob", "carol"]
```

### Требуется
1. Собрать словарь `accounts` с итоговыми балансами.
2. Для каждого имени из `closed` удалить запись через `pop(name, 0)`.
3. Вывести итоговый словарь.

---

## Задача 3. Множества участников

Пример входных данных:
```python
office = {"roman", "olga", "ivan"}
remote = {"ivan", "daria", "roman"}
```

### Требуется
1. Найти пользователей, которые работают и из офиса, и удалённо.
2. Найти пользователей, которые работают только удалённо.
3. Вывести результат с подписями.

---

## Задача 4. Подготовка к comprehensions

Пример входных данных:
```python
visits = [
    ("main", 10),
    ("blog", 4),
    ("main", 7),
    ("help", 2),
]
limit = 10
```

### Требуется
1. Собрать словарь `views_by_page`.
2. Сформировать новый словарь только для страниц, где суммарное количество посещений не меньше `limit`.
3. Вывести оба словаря.
