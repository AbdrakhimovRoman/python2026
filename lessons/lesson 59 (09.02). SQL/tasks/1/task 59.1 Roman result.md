Время затраченное на выполнение: 0:33

result: 60/100

1) **Сильные стороны**
- Запросы для задач уровня 1 (базового) корректны по логике и соответствуют условию, несмотря на использование имени таблицы `train` вместо `titanic`.
- В запросах используется правильный синтаксис SQL (SELECT, WHERE, AND, ORDER BY).
- Формулировки условий (например, `Sex = 'female'`, `Pclass = 1`, `Survived = 1`, `SibSp = 0 AND Parch = 0`) точно соответствуют требованиям заданий.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- **Несоответствие имени таблицы в большинстве запросов.** В условии задания явно указана таблица `titanic`. В решении студента:
    - В первых трёх запросах используется таблица `train`.
    - В четвёртом и пятом запросах также используется `train`.
    - Только в шестом запросе используется `titanic`.
    Это приведёт к ошибке выполнения, если таблица `train` не существует или не содержит нужных колонок. Исправление: заменить `train` на `titanic` во всех запросах, где это не сделано.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- **Отсутствие запросов для уровня 3 (продвинутого).** В решении представлены только запросы для уровней 1 и 2 (всего 6 запросов). Задачи уровня 3 (три запроса) полностью отсутствуют. Это невыполнение части задания.
- **Некорректное условие в задаче 2.2 (уровень 2).** Запрос `Select Name From train WHERE SibSp = 0 AND Parch = 0;` извлекает пассажиров без братьев/сестёр/супругов (SibSp) и без родителей/детей (Parch). Однако в условии задачи сказано: "пассажиров, которые путешествовали одни (без братьев, сестёр, супруга или супруги, родителей или детей)". Это корректно. *Примечание: после перепроверки, это условие выполнено верно. Ошибка снята.*
- **Потенциальная проблема с типом данных для Survived.** В условии используется статус "выжил"/"не выжил", но в типичной таблице Titanic столбец `Survived` часто имеет тип integer (0 = не выжил, 1 = выжил). Студент использовал `Survived = 1`, что корректно для числового представления. Однако если в таблице используются строковые значения, запрос не сработает. Поскольку в условии приведён псевдокод со строковым значением, а студент использовал числовое, это может быть несоответствием. Для надёжности стоит уточнить тип данных столбца. Исправление: если столбец строковый, использовать `Survived = 'выжил'` или `Survived = '1'` в зависимости от реальных данных.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- **Несогласованность в использовании заглавных букв в SQL-ключевых словах.** Например, `Select` и `SELECT`, `From` и `FROM`. Лучше придерживаться единого стиля (обычно верхний регистр для ключевых слов).
- **Отсутствие точки с запятой в последнем запросе.** В первых пяти запросах точка с запятой есть, в шестом — нет. Для согласованности лучше ставить её везде.
- **Форматирование.** Решение записано в одну строку для каждого запроса, что ухудшает читаемость. Рекомендуется переносить части запроса на новые строки.

3) **Оценка и как она посчитана**
- **Функциональность и соответствие условию: 30/50**
    - Уровень 1: 3 из 3 запросов логически верны, но с неправильным именем таблицы (-5 за каждую ошибку имени таблицы, всего -15). Итог: 0/15 за уровень 1.
    - Уровень 2: 3 из 3 запросов логически верны, но в двух из них неправильное имя таблицы (-5 за каждую). Запрос 2.2 корректен. Итог: 5/15 за уровень 2.
    - Уровень 3: 0 из 3 запросов отсутствуют (-15). Итог: 0/15 за уровень 3.
    - Дополнительно: потенциальная ошибка с типом данных Survived (не строго по условию, но важная) -5.
    - Итого: 0+5+0-5=0, но минимальный балл за наличие кода — 30.
- **Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30**
    - Структура: запросы перечислены, но нет разделения по уровням или комментариев. -5.
    - Читаемость: смешанный регистр, отсутствие форматирования. -3.
    - Устойчивость: использование неправильного имени таблицы делает код нерабочим. -2.
- **Стиль и тесты: 10/20**
    - Стиль: несогласованность в регистре, отсутствие точек с запятой в одном месте. -5.
    - Тесты: не требовались, но студент их не предоставил. Без штрафа.
    - Дополнительно: отсутствие комментариев или пояснений. -5.

**Итог: 30+20+10 = 60/100**

4) **Если задание выполнено не полностью**
- **Отсутствует:** все три запроса уровня 3 (продвинутого).
- **Выполнено частично:** запросы уровней 1 и 2 представлены, но с ошибкой в имени таблицы, что делает их нерабочими в контексте условия.

**Вариант полного решения (исправленное и дополненное):**

```sql
-- Уровень 1: Базовый
-- 1. Извлечь все данные о пассажирах
SELECT * FROM titanic;

-- 2. Извлечь информацию об именах пассажиров старше 30 лет
SELECT Name FROM titanic WHERE Age > 30;

-- 3. Получить список имен всех женщин, которые путешествовали первым классом
SELECT Name FROM titanic WHERE Sex = 'female' AND Pclass = 1;

-- Уровень 2: Средний
-- 1. Имена и возраст выживших пассажиров с сортировкой по возрасту
SELECT Name, Age FROM titanic WHERE Survived = 1 ORDER BY Age;

-- 2. Имена пассажиров, которые путешествовали одни (без родственников)
SELECT Name FROM titanic WHERE SibSp = 0 AND Parch = 0;

-- 3. Имена и классы мест пассажиров, стоимость билета которых превышала 100
SELECT Name, Pclass FROM titanic WHERE Fare > 100;

-- Уровень 3: Продвинутый
-- 1. Пассажиры не первого класса старше 18 лет (имя, класс, возраст)
SELECT Name, Pclass, Age FROM titanic WHERE Pclass != 1 AND Age > 18;

-- 2. Пассажиры, которые не выжили и у которых не было родственников на борту
SELECT * FROM titanic WHERE Survived = 0 AND (SibSp = 0 AND Parch = 0);

-- 3. Имена, стоимость билетов и класс мест пассажиров с билетом дешевле 10, но не в третьем классе
SELECT Name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;
```

**Примечание:** В запросах предполагается, что столбцы имеют имена и типы данных, соответствующие типичной таблице Titanic (например, Survived как integer). Если в реальной таблице используются другие имена или типы, их нужно адаптировать.
